<!-- link to saved posts, should be moved to nav bar -->
<%= link_to "Saved posts", lists_path %><br><br>

<!-- when not following anyone, user's @posts = [nil] -->
<% if @posts.include?(nil) %>
  <%= "No posts yet!" %>
<% else %>
  <% @posts.each do |post| %>
    <%= post.user.name%><br>
    <%= post.title%><br><br>
  <% end %>
<% end %>
